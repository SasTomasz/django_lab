[project]
name = "django-lab"
version = "0.1.0"
description = "Playground for Django"
readme = "README.md"
requires-python = ">=3.14,<=3.15"
dependencies = [
    "django>=6.0.3",
    "markdown>=3.10.2",
    "pillow>=12.1.1",
]
